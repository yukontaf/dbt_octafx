name: 'dbt_octafx'
version: '1.0'
config-version: 2
profile: dbt_octafx
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
clean-targets:
- "dbt_packages"
- "target"
models:
  dbt_octafx:
    materialized: view
