metrics:
  - name: conversion_rate
    description: Conversion to payment initiation after promo email
    type: conversion
    label: cr
    type_params: # Required
      conversion_type_params:
        entity: user_id
        calculation: conversions
        base_measure:
          name: promo
          fill_nulls_with: 0
          join_to_timespine: false
        conversion_measure:
          name: payment_system_select
          fill_nulls_with: 0
          join_to_timespine: false
        window: 7 days
